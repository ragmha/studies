# What is CSS?

*   HTML is the content, CSS is the style, the presentation of the content.CSS dictates how the HTML looks

*   CSS is a collection of rules. If certains are met, then a style is applied to it.
